import { useEffect, useState } from "react";
import request from "../api/client.js";

const AdminCommunications = () => {
  const [subscriptions, setSubscriptions] = useState([]);
  const [businesses, setBusinesses] = useState([]);
  const [form, setForm] = useState({ recipientName: "", recipientEmail: "", businessId: "", frequency: "daily", sendTime: "18:00" });
  const [saving, setSaving] = useState(false);

  const load = async () => { const [subscriptionsData, overview] = await Promise.all([request("/admin/report-subscriptions"), request("/admin/overview")]); setSubscriptions(subscriptionsData.subscriptions || []); setBusinesses(overview.businesses || []); };
  useEffect(() => { load().catch((err) => alert(err.message || "Failed to load communications")); }, []);
  const create = async (event) => { event.preventDefault(); try { setSaving(true); const response = await request("/admin/report-subscriptions", { method: "POST", body: JSON.stringify(form) }); setSubscriptions((current) => [response.subscription, ...current]); setForm({ recipientName: "", recipientEmail: "", businessId: "", frequency: "daily", sendTime: "18:00" }); } catch (err) { alert(err.message || "Failed to add recipient"); } finally { setSaving(false); } };
  const toggle = async (subscription) => { const nextStatus = subscription.status === "enabled" ? "admin_disabled" : "enabled"; if (!window.confirm(`Are you sure you want to ${nextStatus === "enabled" ? "enable" : "disable"} reports for ${subscription.recipientEmail}?`)) return; try { const response = await request(`/admin/report-subscriptions/${subscription._id}`, { method: "PATCH", body: JSON.stringify({ status: nextStatus }) }); setSubscriptions((current) => current.map((item) => item._id === subscription._id ? response.subscription : item)); } catch (err) { alert(err.message || "Failed to update recipient"); } };

  return <section className="page-stack max-w-7xl mx-auto"><div className="page-heading"><div><span className="text-sm font-bold uppercase tracking-[0.3em] text-emerald-600">Super Admin</span><h1 className="mt-2 text-4xl font-extrabold text-slate-900">Communications</h1></div><p className="max-w-2xl text-sm text-slate-500">Control scheduled reports, recipients, and email regulation across the platform.</p></div><form onSubmit={create} className="rounded-3xl bg-white p-6 shadow-xl border border-slate-200/80 grid gap-4 md:grid-cols-2 xl:grid-cols-6 items-end"><label className="text-xs font-bold text-slate-500">Name<input required value={form.recipientName} onChange={(event) => setForm({ ...form, recipientName: event.target.value })} className="mt-2 w-full rounded-xl border border-slate-200 px-3 py-2.5" /></label><label className="text-xs font-bold text-slate-500">Email<input required type="email" value={form.recipientEmail} onChange={(event) => setForm({ ...form, recipientEmail: event.target.value })} className="mt-2 w-full rounded-xl border border-slate-200 px-3 py-2.5" /></label><label className="text-xs font-bold text-slate-500">Business<select required value={form.businessId} onChange={(event) => setForm({ ...form, businessId: event.target.value })} className="mt-2 w-full rounded-xl border border-slate-200 px-3 py-2.5"><option value="">Select business</option>{businesses.map((business) => <option key={business._id} value={business._id}>{business.name}</option>)}</select></label><label className="text-xs font-bold text-slate-500">Frequency<select value={form.frequency} onChange={(event) => setForm({ ...form, frequency: event.target.value })} className="mt-2 w-full rounded-xl border border-slate-200 px-3 py-2.5"><option value="daily">Daily</option><option value="weekly">Weekly</option></select></label><label className="text-xs font-bold text-slate-500">Send time<input type="time" value={form.sendTime} onChange={(event) => setForm({ ...form, sendTime: event.target.value })} className="mt-2 w-full rounded-xl border border-slate-200 px-3 py-2.5" /></label><button type="submit" disabled={saving} className="rounded-xl bg-emerald-600 px-5 py-3 text-xs font-bold uppercase text-white disabled:opacity-50">{saving ? "Saving..." : "Add Recipient"}</button></form><div className="overflow-x-auto rounded-3xl bg-white p-6 shadow-xl border border-slate-200/80"><table className="min-w-full text-left text-sm"><thead className="border-b border-slate-200 text-[10px] uppercase tracking-widest text-slate-400"><tr><th className="px-5 py-4">Recipient</th><th className="px-5 py-4">Business</th><th className="px-5 py-4">Schedule</th><th className="px-5 py-4">Status</th><th className="px-5 py-4 text-right">Control</th></tr></thead><tbody className="divide-y divide-slate-100 text-xs">{subscriptions.map((subscription) => <tr key={subscription._id}><td className="px-5 py-4"><strong>{subscription.recipientName || "Unnamed"}</strong><p className="text-slate-400">{subscription.recipientEmail}</p></td><td className="px-5 py-4">{subscription.business?.name || "-"}</td><td className="px-5 py-4">{subscription.frequency} at {subscription.sendTime}</td><td className="px-5 py-4 capitalize">{subscription.status.replace("_", " ")}</td><td className="px-5 py-4 text-right"><button type="button" onClick={() => toggle(subscription)} className="rounded-lg border border-slate-200 px-3 py-2 font-bold">{subscription.status === "enabled" ? "Disable" : "Enable"}</button></td></tr>)}</tbody></table></div></section>;
};

export default AdminCommunications;
